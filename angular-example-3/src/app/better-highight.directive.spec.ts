import { BetterHighightDirective } from './better-highight.directive';

describe('BetterHighightDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHighightDirective();
    expect(directive).toBeTruthy();
  });
});
